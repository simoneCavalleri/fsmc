name: Deploy Documentation & Playground to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'playground/**'
      - 'include/**'
      - 'tools/**'
      - 'CMakeLists.txt'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install MkDocs & Material Theme
        run: |
          pip install mkdocs mkdocs-material pymdown-extensions

      - name: Setup Emscripten SDK
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: 3.1.58

      - name: Build fsmc WebAssembly Module
        run: |
          emcmake cmake -B build-wasm -DFSMC_BUILD_WASM=ON -DFSMC_ENABLE_TESTING=OFF -DFSMC_ENABLE_BENCHMARKS=OFF -DFSMC_ENABLE_EXAMPLES=OFF -DCMAKE_BUILD_TYPE=Release
          cmake --build build-wasm --config Release --target fsmc_wasm
          test -f playground/fsmc.js || cp -f build-wasm/playground/fsmc.js playground/fsmc.js 2>/dev/null || true

      - name: Build MkDocs Site
        run: |
          mkdocs build
          mkdir -p site/playground
          cp -r playground/* site/playground/
          cp -f playground/index.html site/playground/playground.html 2>/dev/null || true

      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'site'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

